// Local imports
import { TabsContent } from '@/components/ui/tabs'

// Types
type Props = Readonly<{}>

export async function GamePageRelated(props: Props) {
	return <TabsContent value={'related'}></TabsContent>
}
